.shared_links
  - if controller_name != 'sessions'
    = link_to "Log in", new_session_path(resource_name)
    %span.devider &#160;|&#160;
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "Meedoen", new_registration_path(resource_name)
    %span.devider &#160;|&#160;
  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "Wachtwoord vergeten?", new_password_path(resource_name)
    %span.devider &#160;|&#160;
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "Geen bevestiging ontvangen?", new_confirmation_path(resource_name)
    %span.devider &#160;|&#160;
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = link_to "Geen ontsluit instructies ontvangen?", new_unlock_path(resource_name)
    %span.devider &#160;|&#160;
  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = link_to "Inloggen met #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider)
    %span.devider &#160;|&#160;
